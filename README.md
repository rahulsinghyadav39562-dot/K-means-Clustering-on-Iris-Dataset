# K-means-Clustering-on-Iris-Dataset
This project applies the K-Means clustering algorithm on the Iris dataset using two features: Sepal Length and Sepal Width. The goal is to divide the dataset into three clusters and analyze grouping behavior.
